<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo -->
    <solid android:color="@color/white" />

    <!-- Borde -->
    <stroke
        android:width="1dp"
        android:color="@color/light_gray" />

    <!-- Radios para esquinas redondeadas -->
    <corners android:radius="12dp" />

    <!-- Padding interno (opcional, útil si usas como background en EditText) -->
    <padding
        android:left="12dp"
        android:top="10dp"
        android:right="12dp"
        android:bottom="10dp" />
</shape>
